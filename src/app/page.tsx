// Route marker — actual UI lives in the persistent AppShell rendered by the
// root layout, so the iframe stays mounted across tab switches.
export default function CompetitorsPage() {
  return null;
}
