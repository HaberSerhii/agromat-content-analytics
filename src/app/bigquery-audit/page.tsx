export const metadata = {
  title: "BigQuery Audit · Agromat",
  robots: { index: false, follow: false },
};

// Hidden route marker. The UI is mounted by AppShell and is intentionally
// omitted from the dashboard navigation.
export default function BigQueryAuditPage() {
  return null;
}

