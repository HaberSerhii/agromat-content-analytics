// Route marker — actual UI lives in the persistent AppShell rendered by the
// root layout, so the sales dashboard stays mounted across tab switches.
export default function SalesPage() {
  return null;
}
